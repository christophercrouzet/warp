# SPDX-FileCopyrightText: Copyright (c) 2024 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: Deploy Sphinx documentation to Pages

on:
  push:
    branches:
      - main
      - release-*
  workflow_dispatch:

concurrency:
  group: deploy-docs
  cancel-in-progress: false

env:
  PYTHONFAULTHANDLER: "1" # Dump tracebacks on fatal signals (SIGSEGV, SIGABRT, etc.)

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          lfs: true
      - name: Fetch gh-pages branch
        # Skip the fetch on a fresh repo where origin has no gh-pages yet
        # (deploy_doc.py's orphan path handles that bootstrap case). When the
        # branch exists, a fetch failure must abort: continuing on stale
        # local state would let `discover_versions()` see an empty tree and
        # the subsequent force-push wipe every previously-deployed version.
        run: |
          if git ls-remote --exit-code --heads origin gh-pages >/dev/null 2>&1; then
            git fetch origin gh-pages:gh-pages
          else
            echo "origin has no gh-pages branch yet — skipping fetch (bootstrap deploy)"
          fi
      - name: Install uv
        uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57  # v8.0.0
        with:
          version: "0.11.3"
      - name: Set up Python
        run: uv python install
      - name: Build Warp without CUDA Support
        run: |
          uv run build_lib.py --no-cuda
      - name: Determine doc version
        run: |
          ref_name="${{ github.ref_name }}"
          if [[ "$ref_name" == "main" ]]; then
            echo "latest" > .doc_version
          elif [[ "$ref_name" =~ ^release-([0-9]+\.[0-9]+)$ ]]; then
            echo "${BASH_REMATCH[1]}" > .doc_version
          else
            echo "::error::Unsupported ref '$ref_name'. Expected 'main' or 'release-MAJOR.MINOR'."
            exit 1
          fi
          echo "Doc version: $(cat .doc_version)"
      - name: Build Sphinx documentation
        run: |
          DOC_VERSION=$(cat .doc_version) uv run --extra docs build_docs.py
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: html-docs
          path: docs/_build/html/
      - name: Deploy to gh-pages
        run: |
          uv run tools/ci/deploy_doc.py --version "$(cat .doc_version)"
          git push --force origin gh-pages
